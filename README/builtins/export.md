`export` is like `export -p`. It returns a list of all the variables names exported in this shell.
`export` should accept arguments but not options.

<h2>Links:</h2>
https://ss64.com/bash/export.html
