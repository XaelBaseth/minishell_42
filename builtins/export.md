`export` is like `export -p`. It returns a list of all the names exported in this shell.
But we don't have to support any option, so we'll use only `export`.

<h2>Links:</h2>
https://ss64.com/bash/export.html
